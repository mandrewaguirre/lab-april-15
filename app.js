function sum (x,y) {
  var add = x + y;
  console.log('The sum of ' + x + ' ' + y + ' is ' + add);

}


function mul (x,y) {
  var multiply = x * y;
  console.log('The product of ' + x + ' ' + y + ' is ' + multiply);
}


var sumM = [mul () , sum (),];
